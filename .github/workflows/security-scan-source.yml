name: Security Scan Source Code (Reusable)

on:
  workflow_call:
    inputs:
      scan_path:
        description: 'Path to scan (directory or file)'
        required: false
        type: string
        default: '.'
      scan_type:
        description: 'Type of scan: fs (filesystem), repo (repository)'
        required: false
        type: string
        default: 'fs'
      severity:
        description: 'Minimum severity level'
        required: false
        type: string
        default: 'CRITICAL,HIGH'
      upload_to_github:
        description: 'Upload results to GitHub Security'
        required: false
        type: boolean
        default: true

jobs:
  security-scan-source:
    name: Security Scan Source Code
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: ${{ inputs.scan_type }}
          scan-ref: ${{ inputs.scan_path }}
          format: 'sarif'
          output: 'trivy-source-results.sarif'
          severity: ${{ inputs.severity }}
          exit-code: '0'  # Don't fail build, just report

      - name: Check SARIF file exists
        id: check-sarif
        if: inputs.upload_to_github && always()
        run: |
          if [ -f "trivy-source-results.sarif" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "SARIF file found"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Warning: SARIF file not found"
          fi

      - name: Upload Trivy results to GitHub Security
        if: inputs.upload_to_github && always() && steps.check-sarif.outputs.exists == 'true'
        id: upload-sarif
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: 'trivy-source-results.sarif'
          wait-for-processing: false
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run Trivy filesystem scan (table format)
        id: trivy-table-scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: ${{ inputs.scan_type }}
          scan-ref: ${{ inputs.scan_path }}
          format: 'table'
          severity: ${{ inputs.severity }}
          exit-code: '0'  # Don't fail build, just report

      - name: Security Scan Summary
        if: always()
        run: |
          echo "### 🔒 Security Scan Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Scan Status | ${{ job.status }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Scan Type | \`${{ inputs.scan_type }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Scan Path | \`${{ inputs.scan_path }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Severity Threshold | \`${{ inputs.severity }}\` |" >> $GITHUB_STEP_SUMMARY
          if [ "${{ inputs.upload_to_github }}" == "true" ]; then
            if [ "${{ steps.check-sarif.outputs.exists }}" == "true" ]; then
              if [ "${{ steps.upload-sarif.outcome }}" == "success" ]; then
                echo "| Results Uploaded | ✅ Yes (GitHub Security) |" >> $GITHUB_STEP_SUMMARY
              else
                echo "| Results Uploaded | ⚠️ Attempted (may have failed) |" >> $GITHUB_STEP_SUMMARY
              fi
            else
              echo "| Results Uploaded | ❌ No (SARIF file not found) |" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "| Results Uploaded | ⏭️ Disabled |" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> 🔍 Trivy scans for vulnerabilities in source code. Results are filtered to show only \`${{ inputs.severity }}\` severity issues." >> $GITHUB_STEP_SUMMARY
          echo "> 📋 Scan results are displayed in table format in the workflow logs and uploaded as SARIF to GitHub Security (if enabled)." >> $GITHUB_STEP_SUMMARY
          if [ "${{ inputs.upload_to_github }}" == "true" ]; then
            echo "> 📊 SARIF results are available in the GitHub Security tab for detailed review." >> $GITHUB_STEP_SUMMARY
          fi